# editable-decrypt

Make decrypted HIDDEN-CAP blocks editable: view-only by default with an Edit action that opens the Quill editor and re-encrypts on save, plus an Edit shortcut on the post-encrypt success screen.
